# Frontend - React Dashboard

## Install & Run
```bash
npm install
npm start
```

The app proxies API operations to `http://localhost:8000`. Use the seeded `EMP-101` employee ID for demo data, adjust identity as needed.
